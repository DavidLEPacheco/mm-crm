# Mazar Martin Pipeline — environment variables
# Copy this file to .env and fill in real values. Never commit .env.
#
# Alternatively, the scripts also read .mm_credentials in the script directory:
#   pipeline/scripts/.mm_credentials  with lines like  KEY=VALUE
# That file is gitignored too.

# Gmail IMAP App Password (from Google Account → Security → App Passwords).
# Account is mazarmartinapp@gmail.com — used by scrape_gmail.py
GMAIL_APP_PASSWORD=
